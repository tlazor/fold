services:
  fold:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    volumes:
      - ../data:/fold/data:ro
    command: /bin/bash
    stdin_open: true
    tty: true
